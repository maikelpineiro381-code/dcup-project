# DCUP Project

Este proyecto contiene tres servicios independientes que forman el ecosistema DCUP:

- **Blockchain Core** → `blockchain/MCoinsBlockChain.py`
- **Wallet Server** → `wallet/WalletServer.py`
- **Exchange Server** → `exchange/ExchangeServer.py`

---

## 🚀 Despliegue en Render

### 1. Subir a GitHub
Clona este repo en tu máquina y súbelo a GitHub:

```bash
git init
git add .
git commit -m "DCUP project inicial"
git branch -M main
git remote add origin https://github.com/tuusuario/dcup-project.git
git push -u origin main
